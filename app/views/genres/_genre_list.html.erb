<% movie_ids = @genre.movie_ids %>
<% movie_ids.each_slice(5) do |batch| %>
  <div class="posters-wrapper">
    <% batch.each do |movie_id| %>
      <% @movie = Movie.find(movie_id) %>
      <div class="poster">
        <%= link_to image_tag(@movie.photo, class: 'img-responsive'), movie_path(@movie) %>
      </div>
    <% end %>
  </div>
<% end %>
