<% @genres.each do |genre| %>
  <div class="row carousel-row">
    <% if genre.name == "Action" %>
      <h3>
        <a href=<%= genre_path(genre) %>>Action &amp; Adventure<i class="fa fa-chevron-right"></i></a>
      </h3>
    <% else %>
      <h3>
        <a href=<%= genre_path(genre) %>><%= genre.name %><i class="fa fa-chevron-right"></i></a>
      </h3>
    <% end %>
    <div class="slider-<%= genre.name %>">
      <div class="border-left"></div>
      <div class="border-right"></div>
      <ul>
        <% movie_ids = genre.movie_ids %>
        <% movie_ids.each_slice(@batch) do |batch| %>
          <li class="max-92">
            <% batch.each do |movie_id| %>
              <% @movie = Movie.find(movie_id) %>
              <div class="poster">
                <div class="poster-inner">
                  <%= link_to image_tag(@movie.photo), movie_path(@movie) %>
                </div>
              </div>
            <% end %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
<% end %>


        <%# movie_ids = genre.movie_ids %>
        <%# movie_ids.each do |movie_id| %>
          <%# @movie = Movie.find(movie_id) %>
          <!-- <div class="poster">
            <div class="poster-inner">
              <%#= link_to image_tag(@movie.photo), movie_path(@movie) %>
            </div>
          </div> -->
        <%# end %>






